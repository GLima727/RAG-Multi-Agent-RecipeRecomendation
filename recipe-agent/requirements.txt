# Core RAG stack
chromadb>=0.5.0
sentence-transformers>=3.0.0
datasets>=2.20.0

# CLI
rich>=13.7.0

# LLM providers — install the ones you need
anthropic>=0.40.0          # provider: anthropic  (ANTHROPIC_API_KEY)
openai>=1.40.0             # provider: openai     (OPENAI_API_KEY)
                           # provider: groq       (GROQ_API_KEY)  — reuses openai SDK
google-generativeai>=0.8.0 # provider: gemini     (GEMINI_API_KEY)
